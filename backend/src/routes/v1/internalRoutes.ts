import { Router } from 'express';

const router = Router();

/**
 * @summary
 * Internal (authenticated) routes configuration for V1
 *
 * @description
 * Authenticated endpoints that require valid authentication.
 * Feature-specific routes will be added here as they are implemented.
 */

/**
 * @todo Add feature-specific internal routes here
 * Example:
 * import quizRoutes from '@/api/v1/internal/quiz/routes';
 * router.use('/quiz', quizRoutes);
 */

export default router;
